import { ColumnSortableDirective } from './column-sortable.directive';

describe('ColumnSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnSortableDirective();
    expect(directive).toBeTruthy();
  });
});
